{
  "ignoreRoot": [".git"],
  "watch": ["src", "node_modules/@pyxismedia"],
  "ext": "ts,graphql,json",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node --project tsconfig.development.json -r tsconfig-paths/register src/main.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
